import { Alert } from 'react-native';

export const exportChartToPDF = (chartData, chartType) => {
  Alert.alert(
    'Coming Soon',
    'PDF export will be available in Phase 2-3',
    [{ text: 'OK', style: 'default' }]
  );
};

export const exportReportToPDF = (reportData) => {
  Alert.alert(
    'Coming Soon',
    'PDF export will be available in Phase 2-3',
    [{ text: 'OK', style: 'default' }]
  );
};
